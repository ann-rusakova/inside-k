# Inside K

Скиллы для Claude Code, Codex и Cursor: гайды интервью, JTBD-артефакты, анализ качественных
данных, опросы, редактура текстов и другие рабочие инструменты отдела UX-исследований Контура.
Исходники скиллов находятся в `.agents/skills/`; общие инструкции по работе с репозиторием — в
[AGENTS.md](AGENTS.md).

- **Работа в репозитории.** Claude Code, Codex и Cursor читают скиллы прямо из `.agents/skills/`
  (`.claude/skills` и `.cursor/skills` — симлинки на ту же папку). Сборка для этого не нужна —
  открывайте репозиторий и работайте.
- **Общие исходники.** Редполитика хранится в [`references/editorial-policy/`](references/editorial-policy).
  Сборщик встраивает её в Figma-плагин; вручную правятся только исходники.
- **11 скиллов**, каждый — одна задача с явными триггер-фразами и границами «когда НЕ применять»,
  чтобы agent не путал соседние сценарии.
- **Плагины Figma**, начиная с [`figma-plugins/simple-editor`](figma-plugins/simple-editor) — та же
  редполитика прямо в Figma, без переключения в чат.

```
you: "Оцени вот этот текст экрана по редполитике"
→ Claude сам подхватывает full-analysis-text: индекс качества 0–10 + список замечаний с цитатами
```

## Как пользоваться

1. Склонируйте репозиторий и откройте его в Claude Code, Codex или Cursor — все три читают
   `.agents/skills/` (или симлинки на неё) без дополнительной установки.
2. Просто опишите задачу в чате своими словами («напиши гайд интервью», «сведи анализ ЮТ»,
   «оцени текст по редполитике») — агент сам подхватит нужный скилл по описанию в таблице ниже.
   Явно называть скилл не обязательно.
3. Если нужна точечная редактура текста в макете Figma без переключения в чат — используйте
   Figma-плагин [«Просто редактор»](#плагины), он работает по той же редполитике.
4. Меняете скилл или редполитику сами — прочитайте [AGENTS.md](AGENTS.md) о конвенциях, после
   правки `references/editorial-policy/` или скиллов `only-editor`/`full-analysis-text`
   запустите `npm run build:simple-editor`, чтобы обновить базу знаний Figma-плагина (см.
   [«Сборка и форматирование»](#сборка-и-форматирование)).

## Скиллы

| Скилл                                                                                     | Для чего                                                                          |
| ------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------- |
| [`design-guide`](.agents/skills/design-guide)                                             | Гайд для UX-интервью (Крюгер, Квале)                                              |
| [`jtbd`](.agents/skills/jtbd)                                                             | Job Statement, Job Story, Forces of Progress, Job Map из транскриптов             |
| [`qualitative-analysis-rules`](.agents/skills/qualitative-analysis-rules)                 | Стандарт сведения анализа интервью/ЮТ в таблицу «вопрос × респондент»             |
| [`survey`](.agents/skills/survey)                                                         | Проектирование количественного опроса → `.docx`                                   |
| [`only-editor`](.agents/skills/only-editor)                                               | Точечная правка одного фрагмента интерфейсного текста                             |
| [`full-analysis-text`](.agents/skills/full-analysis-text)                                 | Полный аудит текста с индексом качества 0–10                                      |
| [`figma-spec-notes`](.agents/skills/figma-spec-notes)                                     | Техспека к макету прямо в Figma, текстовыми блоками рядом с экраном               |
| [`brainstorm-experiments-existing`](.agents/skills/brainstorm-experiments-existing)       | Дешёвые эксперименты для проверки гипотез существующего продукта                  |
| [`brainstorm-experiments-new`](.agents/skills/brainstorm-experiments-new)                 | Претотипы и XYZ-гипотезы для проверки идеи нового продукта                        |
| [`opportunity-solution-tree`](.agents/skills/opportunity-solution-tree)                   | Opportunity Solution Tree: от цели к возможностям, решениям и экспериментам       |
| [`objective-prompt-rewriter`](.agents/skills/objective-prompt-rewriter)                   | Аудит и переписывание промптов в нейтральную, небиасящую формулировку             |

Технические правила для агента — в [AGENTS.md](AGENTS.md), принципы работы отдела — в
[SOUL.md](SOUL.md), тон и голос коммуникации — в [TONE-OF-VOICE.md](TONE-OF-VOICE.md).

## Начало работы

```bash
git clone https://github.com/ann-rusakova/inside-k.git
cd inside-k
claude   # или откройте папку в Cursor/Codex — все три читают AGENTS.md
```

Симлинки `.claude/skills` и `.cursor/skills` ведут в `../.agents/skills` — исходники общие,
ничего собирать для работы в репозитории не нужно. Проверить ссылки:

```bash
ls -la .claude/skills .cursor/skills   # обе ссылки -> ../.agents/skills
```

Новый скилл добавляется как обычная папка `.agents/skills/<name>/SKILL.md` — она сразу видна и
по путям `.claude/skills/<name>` и `.cursor/skills/<name>` — это одна и та же папка на диске (см. правила в
[AGENTS.md](AGENTS.md#скиллы)).

Cursor также поддерживает `.agents/skills/` напрямую: [документация Cursor](https://cursor.com/docs/skills).
Ссылка `.cursor/skills` даёт явную точку входа для Cursor; правьте оригиналы в `.agents/skills/`.

## Плагины

[`figma-plugins/simple-editor`](figma-plugins/simple-editor) — «Просто редактор» (Simple Editor), Figma-плагин для
UX-редактуры текста прямо в макете: полный анализ текста (индекс качества 0–10) и точечный
редактор одного фрагмента (переформулировать/сократить/четче/дружелюбнее), по тем же правилам,
что и скиллы `full-analysis-text`/`only-editor`.

- **Установка (локально):** Figma Desktop → `Plugins` → `Development` →
  `Import plugin from manifest...` → выбрать `figma-plugins/simple-editor/manifest.json`.
  Если плагин уже был импортирован из прежней папки `plugins/simple-editor/` (переехала в
  `figma-plugins/simple-editor/`) — удалите старую запись в `Development` и импортируйте manifest
  заново по новому пути, иначе Figma продолжит запускать устаревшую закешированную сборку.
- **База знаний** — [`references/editorial-policy/`](references/editorial-policy) (реестр источников —
  [`references/sources.json`](references/sources.json)), вшивается в `ui.html` сборщиком
  `tools/update-simple-editor.js` командой `npm run build:simple-editor` (из корня) —
  запускать после правки файлов в `references/editorial-policy/`, `sources.json` или двух скиллов ниже.
- **Инструкции режимов** — напрямую из
  [only-editor/SKILL.md](.agents/skills/only-editor/SKILL.md) для редактора и вариантов правки,
  [full-analysis-text/SKILL.md](.agents/skills/full-analysis-text/SKILL.md) для полного анализа.
  Сборщик вшивает весь текст инструкций без YAML-метаданных и связывает Markdown-ссылки с
  общей базой знаний. В API отправляются выбранный скилл и полные тексты его источников.
  Плагин добавляет требования к JSON и выбранной операции; для фрейма инструкции применяются
  к каждому блоку в контексте экрана. Изменение скилла сбрасывает применимость старого кеша.
  После сборки перезапустите плагин в Figma; во время работы он не читает файлы репозитория.
- **Провайдеры:** Claude API (нужен ключ `sk-ant-...` с console.anthropic.com, не подписка
  Claude Pro/Max), OpenAI API или свой OpenAI-совместимый сервер. Его домен должен быть
  разрешён в `manifest.json`. Mock доступен в браузерном предпросмотре `ui.html` вне Figma.
- **Публикация в Figma Community:** `Plugins` → `Development` → плагин → `Publish new release`,
  форма публикации подтягивает `name`/`main`/`ui` из `manifest.json`; сроки модерации определяет Figma.
- Ключи API хранятся только в `figma.clientStorage` пользователя, никогда не в репозитории.
  При переименовании сохранены `id: kontur-editor` и ключи хранилища `kontur-editor-*`,
  чтобы плагин продолжал читать прежние настройки и сохранённые API-ключи.

## Сборка и форматирование

Нужен Node.js 22 или новее. Установите инструменты разработки командой `npm ci`.
Prettier — зависимость только для разработки; плагину npm-пакеты не нужны.

Собрать базу знаний Figma-плагина из корня репозитория:

```bash
npm run build:simple-editor
```

Команда встраивает скиллы и редполитику в `ui.html`, проверяет манифест и синтаксис кода
Figma-плагина. При ошибке останавливается. После успешного завершения закройте и заново
запустите плагин в Figma — во время работы он не перечитывает файлы репозитория.

Отдельные команды:

```bash
npm run format        # форматирование по .prettierrc
npm run format:check  # проверка форматирования без изменения файлов
```

Настройки Prettier — в `.prettierrc`, исключения — в `.prettierignore`.
Сборщик автоматически форматирует весь `figma-plugins/simple-editor/ui.html` после встраивания
базы знаний. Файл также обрабатывается командой `npm run format`.
После изменения или форматирования любого скилла или справочника
запускайте `npm run build:simple-editor`.
Проверка актуальности сборки: `node tools/update-simple-editor.js --check`.
Синтаксис кода и подключение скиллов проверяются этим же скриптом до записи результата.

## Структура репозитория

Карта каталогов и файлов — для тех, кто впервые открывает репозиторий.

### Корень

| Файл                 | Описание                                                                                      |
| --------------------- | ----------------------------------------------------------------------------------------------- |
| `README.md`          | Этот файл: обзор, как пользоваться, таблица скиллов, плагины, сборка, структура.               |
| `AGENTS.md`           | Технические правила для агента (Claude/Codex/Cursor): структура, конвенции скиллов, границы.   |
| `CLAUDE.md`           | Импортирует общие инструкции из `AGENTS.md` (`@AGENTS.md`).                                    |
| `SOUL.md`             | Тон и принципы работы отдела: зачем скиллы существуют, как они не должны себя вести.            |
| `TONE-OF-VOICE.md`    | Голос и тон коммуникации в текстах Контура: как говорим с клиентом, какие слова используем.     |
| `SECURITY.md`         | Политика безопасности: как сообщить об уязвимости, работа с API-ключами, scope.                |
| `DESIGN.md`           | Дизайн-система Figma-плагина: CSS-переменные, компоненты, чеклист перед PR с UI.                |
| `package.json`        | Единственный npm-манифест репозитория; скрипт `build:simple-editor` собирает базу знаний плагина.    |
| `.prettierrc`         | Настройки Prettier: отступы, кавычки, переносы строк.                                          |
| `.prettierignore`     | Исключения форматирования: зависимости и lock-файл.                                            |
| `package-lock.json`   | Закреплённое дерево зависимостей инструментов разработки.                                      |
| `.gitignore`          | Стандартные исключения (macOS, `node_modules/`, `.env*`, IDE-файлы).                            |

### `.agents/skills/` — источник правды для скиллов

`.claude/skills` и `.cursor/skills` — симлинки на эту папку, редактировать нужно только здесь.

#### `design-guide/` — гайд для UX-интервью

| Файл                                   | Описание                                                                            |
| --------------------------------------- | -------------------------------------------------------------------------------------- |
| `SKILL.md`                             | Проектирует структурированный гайд для UX-интервью по методологии Крюгера и Квале.   |
| `examples/example-interview-guide.md`  | Учебный пример гайда для глубинного интервью (сервис проверки контрагентов).        |
| `examples/example-ux-test-guide.md`    | Учебный пример сценария UX-тестирования (B2B-поиск похожих компаний).               |
| `reference/guide-structure.md`         | Шаблоны и примеры формулировок для каждого блока гайда.                             |
| `reference/krueger-principles.md`      | Обоснование методологии по Крюгеру и Кейси (Focus Groups, 5th ed.).                  |
| `reference/kvale-question-design.md`   | Проектирование вопросов по Стейнару Квале («Исследовательское интервью»).           |
| `reference/question-rules.md`          | Правила формулировки вопросов для UX-интервью, адаптация Крюгера.                   |

#### `jtbd/` — артефакты Jobs To Be Done

| Файл                                    | Описание                                                                                 |
| ---------------------------------------- | ------------------------------------------------------------------------------------------ |
| `SKILL.md`                              | Строит Job Statement, Job Story, Forces of Progress и Job Map из транскриптов интервью.  |
| `references/dve-shkoly.md`              | Две школы JTBD (Христенсен vs Ульвик) и как собрать все четыре артефакта вместе.          |
| `references/job-statement.md`           | Job Statement — описание прогресса, который человек хочет совершить (по Капаеву).         |
| `references/job-story.md`               | Формат и структура Job Story.                                                              |
| `references/forces-of-progress.md`      | Модель Forces of Progress: push/pull, тревога, привычка.                                  |
| `references/job-map.md`                 | Job Map (Тони Ульвик) — шаги функциональной работы.                                       |
| `references/rabota-s-transkriptami.md`  | Как работать с транскриптами, не додумывая за респондента — единица анализа.              |

#### Остальные скиллы (по одному `SKILL.md`, без `references/`)

| Файл                                          | Описание                                                                                         |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| `qualitative-analysis-rules/SKILL.md`         | Стандарт отдела для сведения анализа интервью/ЮТ в таблицу «вопрос/гипотеза × респондент».         |
| `survey/SKILL.md`                             | Проектирует количественный опрос для продуктового исследования, сохраняет в `.docx`.               |
| `only-editor/SKILL.md`                        | Точечная правка одного фрагмента интерфейсного текста (кнопка, тост, заголовок) по редполитике.    |
| `full-analysis-text/SKILL.md`                 | Полный аудит связного текста: индекс качества 0–10, замечания с цитатами.                          |
| `figma-spec-notes/SKILL.md`                   | Техспека к выделенному макету текстовыми блоками прямо на холсте Figma.                            |
| `brainstorm-experiments-existing/SKILL.md`    | Дешёвые эксперименты (прототипы, A/B, спайки) для проверки гипотез существующего продукта.         |
| `brainstorm-experiments-new/SKILL.md`         | Претотипы и XYZ-гипотезы для проверки спроса на идею нового продукта.                              |
| `opportunity-solution-tree/SKILL.md`          | Opportunity Solution Tree: от желаемого результата к возможностям, решениям, экспериментам.        |
| `objective-prompt-rewriter/SKILL.md`          | Аудит и переписывание черновых промптов в нейтральную, небиасящую формулировку на английском.      |

### `references/` — общая база знаний (общий источник)

Единственный источник правил; на него ссылаются скиллы `only-editor`/`full-analysis-text` и
собирает Figma-плагин через `tools/update-simple-editor.js`.

| Файл           | Описание                                                                                            |
| --------------- | -------------------------------------------------------------------------------------------------- |
| `sources.json` | Реестр источников: заголовок, URL, файл, флаг `editorFocus` — единственный список, не дублируется. |

#### `editorial-policy/` — редполитика Контура

| Файл                                        | Описание                                                                            |
| --------------------------------------------- | ---------------------------------------------------------------------------------------- |
| `svodnye-pravila-po-gruppam.md`             | Сводный документ по всем группам правил — основной источник для «Полного анализа».   |
| `osnovnye_pravila_teksta_v_interfeise.md`   | Базовые правила текста в интерфейсе: заголовки, кнопки, подтверждения, тон.          |
| `screen-typography-rules.md`                | Типографика: неразрывные пробелы, кавычки, тире/дефис/минус.                         |
| `disputed-words-rules.md`                   | Спорные слова и штампы.                                                              |
| `letters-rules.md`                          | Регистр, капс, «вы» со строчной.                                                     |
| `numbers-currency-rules.md`                 | Числа, даты, валюта.                                                                 |
| `signs-rules.md`                            | Знаки препинания и спецсимволы.                                                      |
| `simple-words-rules.md`                     | Простой язык: синтаксис и лексика.                                                   |
| `company-names-rules.md`                    | Написание названий компаний.                                                         |
| `laws-services-names-rules.md`              | Написание названий законов и сервисов.                                               |

### `figma-plugins/simple-editor/` — Figma-плагин «Просто редактор»

Содержит только код плагина; данные и сборка вынесены наружу (`references/`, `tools/`).

| Файл            | Описание                                                                                                  |
| ---------------- | ----------------------------------------------------------------------------------------------------------- |
| `manifest.json` | Манифест Figma-плагина: точки входа, `networkAccess.allowedDomains`.                                       |
| `code.js`       | Backend-логика плагина (доступ к макету, применение правок к текстовому слою).                            |
| `ui.html`       | UI плагина; между `<!-- BUNDLED:START/END -->` вшита база знаний (генерируется, не редактировать руками). |

### `tools/` — сборка

| Файл                 | Описание                                                                                                                        |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| `update-simple-editor.js` | Собирает базу знаний Figma-плагина из `.agents/skills/` и `references/`, проверяет манифест, код и подключение скиллов. Запуск: `npm run build:simple-editor`; `--check` — без записи. |

### `.claude/` — конфигурация Claude Code

Технические инструкции для агентов находятся в корневом `AGENTS.md`.

| Файл                                    | Описание                                                                                                                                                  |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.claude/skills`                        | Симлинк на `.agents/skills` — та же папка под путём, который ждёт Claude Code.                                                                            |
| `.claude/settings.json`                 | Allowlist команд и подключение хука-напоминания о сборке скиллов и справочников.                                                                          |
| `.claude/hooks/remind-content-build.js` | Хук Claude Code: напоминает о сборке при изменении любого скилла или справочника, включая пути через симлинки; принимает абсолютные и относительные пути. |

### `.cursor/` — подключение Cursor

`.cursor/skills` — симлинк на `../.agents/skills`, как и `.claude/skills`.
Все скиллы и их локальные справочники доступны через общий каталог исходников.
