@AGENTS.md

## Claude-specific

- Скиллы лежат в `.agents/skills/`; `.claude/skills` — симлинк на неё. Правь только оригинал в `.agents/`.
- Новый скилл: `.agents/skills/<name>/SKILL.md` + `references/`, `examples/`, `scripts/`, `assets/` по необходимости.
