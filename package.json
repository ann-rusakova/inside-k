{
  "name": "inside-k",
  "version": "1.0.0",
  "private": true,
  "description": "UX-исследования и редактура Контура: скиллы + Figma-плагины",
  "scripts": {
    "update:plugin": "node tools/update-k-editor.js",
    "build:k-editor": "node tools/update-k-editor.js",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "prettier": "3.9.6"
  }
}
